# Used only by Jest (see tests/env.setup.ts). SMTP is mocked, never dialed.
NOTIFICATION_SERVICE_NAME=notification-test
NOTIFICATION_PORT=3996
KAFKA_BROKER=localhost:9092
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=test@battery-passport.local
NOTIFY_EMAIL_TO=ops-test@battery-passport.local
